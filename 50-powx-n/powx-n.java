class Solution {
    public double myPow(double x, int n) {
        // int z=1;
        // if(n==0)
        // return 1;
        // else
        // return 2;

        // return z*myPow(x,n-1);
        return Math.pow(x,n);
        
    }
}